## Pre-compact backup [2026-07-08 11:43]

### Key decisions
- Added `OptionChainRequest`, `OptionQuoteRequest`, `OptionKlineItem` structs to `quote_requests.rs`
- `OptionKlineRequest.option_query` changed from `Vec<OptionQueryItem>` to `Vec<OptionKlineItem>`
- `OptionQueryItem` kept separate for OptionTradeTicks and OptionTimeline interfaces (different semantics)
- Expiry timezone: America/New_York — `2024-01-19T00:00:00 NY = 1705647600000ms`
- `#[deprecated(since = "0.5.1")]` stays as-is (items were deprecated in 0.5.1)

### Current state
- Version bumped to 0.5.2 (from 0.5.1)
- 267 unit tests pass, 0 failures
- CHANGELOG updated
- Commit made with all changes

### Don't lose
- `pub use quote_requests::*` in `model/mod.rs` provides full re-export — new structs are automatically exported
- The fix for compile error was a formatting issue where `/// comment` and `#[derive(...)]` got merged onto one line during Edit
- example file uses `unwrap_or_else` pattern (not `?` inside match arms) because function returns `Result`
- All work for this session is complete and committed
